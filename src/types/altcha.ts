export interface AltchaWidget extends HTMLElement {
  reset(): void;
  getToken(): string | null;
}

export interface AltchaWidgetMethods {
  reset(): void;
  getToken(): string | null;
}

export {};
